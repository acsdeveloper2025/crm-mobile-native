import React from 'react';
import { View, Text } from 'react-native';

const InteractiveMap: React.FC<any> = () => (
  <View>
    <Text>Interactive map is not enabled in this build.</Text>
  </View>
);

export default InteractiveMap;
