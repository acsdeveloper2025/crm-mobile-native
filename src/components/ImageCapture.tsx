import React from 'react';
import { View, Text } from 'react-native';

const ImageCapture: React.FC<any> = () => (
  <View>
    <Text>Legacy image capture component is disabled in native.</Text>
  </View>
);

export default ImageCapture;
