import React from 'react';
import { View, Text } from 'react-native';

const CompactImageDisplay: React.FC = () => (
  <View>
    <Text>Image preview unavailable.</Text>
  </View>
);

export default CompactImageDisplay;
