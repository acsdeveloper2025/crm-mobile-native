import React from 'react';
import { View, Text } from 'react-native';

const OfflineStorageDemo: React.FC = () => (
  <View>
    <Text>Offline storage demo is disabled in the native build.</Text>
  </View>
);

export default OfflineStorageDemo;
